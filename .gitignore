# ------------------------------------------------------------------
# Dart / Flutter
# ------------------------------------------------------------------
.dart_tool/
.packages
.pub-cache/
.pub/
build/
**/build/
.flutter-plugins
.flutter-plugins-dependencies
**/doc/api/
.fvm/

# Fichiers de lock : conservés pour les apps, ignorés pour les packages/lib.
# Monorepo de packages -> on ignore par défaut (décommenter si besoin de figer).
# pubspec.lock

# Overrides générés par melos / pub workspaces
pubspec_overrides.yaml
**/pubspec_overrides.yaml
.melos_tool/

# ------------------------------------------------------------------
# Génération de code (build_runner) — régénérable
# ------------------------------------------------------------------
# NB: on VERSIONNE les .g.dart/.freezed.dart pour faciliter la consommation
# du package. Décommenter pour les exclure si le CI les régénère.
# *.g.dart
# *.freezed.dart
# *.gr.dart
# *.mocks.dart

# ------------------------------------------------------------------
# IDE & OS
# ------------------------------------------------------------------
.idea/
*.iml
*.ipr
*.iws
.vscode/
.DS_Store
Thumbs.db
*.swp
*~

# ------------------------------------------------------------------
# Plateformes natives (générées par `flutter create`)
# ------------------------------------------------------------------
**/.gradle/
**/local.properties
**/*.log
**/Pods/
**/.symlinks/
**/xcuserdata/
*.lock.hcl

# ------------------------------------------------------------------
# Divers / temporaire
# ------------------------------------------------------------------
*.tmp
coverage/
.env
.env.*
!.env.example
