---
scope: Epics E-STUDY-UI + E-MULTI-EDIT (21 FR / 10 NFR du PRD prd-zcrud-study-ui-2026-07-16)
purpose: Spine de consistance pour les stories des 2 epics
altitude: epic
updated: 2026-07-16T18:07
---

- (constraint) HÉRITÉ LIANT (jamais renuméroté): AD-1..16 (spine fondateur 2026-07-09: acyclique/CORE OUT=0, réactivité granulaire, codegen, extension AD-4, ports, bindings, ZCodec, ZListRenderer, offline-first+SRS séparé, défensif, Either/ZFailure, zéro secret, RTL-a11y-l10n, pureté couches, multi-manager, ACL+curseur).
- (constraint) HÉRITÉ LIANT: AD-17..28 (spine study 2026-07-12: kernel study multi-packages, ZStudyFolder, ZSyncMeta hors-entité + clés réservées + gate, dépôt générique bi-topologie, cascade déclarative bornée ≤450, ZSm2Scheduler source unique, runtimes de session PURS zéro écriture SM-2, ZStudySessionConfig forme unique, apparence IFFD scoping isolé + ZFeatureAvailability, communauté=extension, migration flat→canonique adapter-only, contenus rich-text typés AD-28).
- (constraint) HÉRITÉ LIANT: AD-29..32 (spine UI 2026-07-16: packages UI purs étagés, présentation dérivée du breakpoint + présentateurs manager en bindings, grille clamp min 1 + LayoutBuilder local, garde dirty + toast par port) + convention transverse enums>booléens. Nouveaux AD de ce spine: AD-33+.
- (constraint) Entrées liantes: PRD final prd-zcrud-study-ui-2026-07-16 (FR-SU1..21, NFR-SU1..10, OA-1..6 à trancher ici), brief final + addendum. Lecture SEULE de /home/zakarius/DEV/lex_douane et /home/zakarius/DEV/iffd.
- (decision) Mode: COACHING INVESTIGUÉ (investigations OA-1..6 en sous-agents lecture seule d'abord, puis décisions AD-33+ une à une avec alternatives). Livrable: SPINE SEUL (pas de deck/solution design).
- (version) Vérif web 2026-07-16: flutter_card_swiper 7.2.0 = dernière (160 pts, maintenu lent); confetti 0.8.0 = dernière (dormant 21 mois, trivial/remplaçable); graphite 1.2.1 = dernière (builder de nœud custom + defaultCellSize/cellPadding CONFIRMÉS); flutter_math_fork 0.7.4 (dormant 14 mois, pur Dart sans WebView, plan B: flutter_tex 5.2.7 Math2SVG); syncfusion_flutter_pdf 34.1.31 (très actif, PdfBitmap PNG confirmé).
- (direction) OA-1 GO (web): LaTeX→PDF via rendu off-screen flutter_math_fork → PNG (RepaintBoundary détaché ou package screenshot; pièges: !debugNeedsPaint, FontLoader polices KaTeX en test, pixelRatio ≥3, CanvasKit sur web) → PdfBitmap/drawImage. À encapsuler derrière un port de rasterisation LaTeX neutre dans la couche export; risque résiduel moyen-faible (raster non vectoriel, CI headless); deps dormantes à garder isolées dans leurs satellites (AD-1).
- (version) Veille web (2026-07-16): flutter_card_swiper 7.2.0, confetti 0.8.0, graphite 1.2.1, flutter_math_fork 0.7.4, syncfusion_flutter_pdf 34.1.31 — toutes = dernière version publiée, ^ du projet aligné. confetti/flutter_math_fork/graphite DORMANTS (14-21 mois) mais viables → isoler dans satellites (AD-1), jamais dans core.
- (decision) OA-1 TRANCHÉ (GO v1): rendu LaTeX PDF via chaîne flutter_math_fork → capture off-screen PNG (pixelRatio≥3, FontLoader KaTeX) → PdfBitmap+drawImage syncfusion. Encapsulé derrière un PORT neutre ZLatexRasterizer dans la couche export (testable widget-test + golden). Risque résiduel moyen-faible: fragilité off-screen headless (frame pump, timing GPU, polices). Plan B documenté: flutter_tex Math2SVG.
- (version) OA-2 (préparé): graphite expose custom node builder (widget arbitraire) + defaultCellSize/cellPadding — suffisant pour label riche dans le graphe; taille de cellule à fixer, pas de mesure auto → contrainte de rendu à cadrer.
- (constraint) FAITS IFFD (OA-5): evaluateFlashcardAnswer(question, userAnswer, correctAnswer, questionType, timeTakenSeconds, previousHints[]) → score 1-5 en TEXTE BRUT (int.tryParse + borne 1..5); repli déterministe: QCM/VF localement 5|1, ouvert/exercice → 3 neutre; 'je ne sais pas' → 1. generateFlashcardHint(question, correctAnswer, existingHints[]) → indice texte; indices = stock local d'abord (hint + accumulés), IA à épuisement avec existingHints anti-répétition.
- (constraint) FAITS IFFD (génération): 3 modes = pagesContents Map<int,String> / sujets+tags / texte libre; réponse = JSON array de flashcards (jsonMode); répartition Map<QuestionType,double> filtrée >0 avec DEUX défauts divergents à unifier ({open5,qcm4,vf3,ex3} écran vs {qcm5,vf4,open3,ex3} modèle); instructions + choix de modèle existent mais le sélecteur showQuestionsTypesSelectionDialig N'EST PAS CÂBLÉ dans le dialog actuel — le port zcrud doit modéliser ces champs proprement (pas de détournement d'endpoint summaryModel).
- (constraint) FAITS IFFD (cascade+streak): purge RepetitionInfo = batchDelete(where flashcardId) en UN WriteBatch (PAS de lots de 10 — mythe corrigé; exceptions avalées + smartDelete non await = purge partielle possible → à durcir en port). Streak: minuit LOCAL, incrément si écart exactement 1 jour, sinon remise à UN (jamais 0), déjà-compté-aujourd'hui = no-op; persisté {currentStreak:int, lastStudyDate:epoch ms}; skip en listOnly; snackbar 🔥.
- (constraint) FAITS LEX (OA-3): ordre manuel = entité HORS-ITEM FolderContentsOrder{folderId, orders: Map<sectionKey,List<id>>} snake_case, sectionKey='<kind>[/<subFolderId>]', collection PERSONNELLE users/{uid}/study_content_orders (jamais partagée); applyOrder PUR (ordre mémorisé d'abord, nouveaux appendés stables, fantômes ignorés); double affordance drag + monter/descendre (menu ⋮) partageant _persistOrder; annonce Semantics au succès.
- (constraint) FAITS LEX (OA-5): génération = FlashcardGenerationRequest{source, count 1..50, typesDistribution Map<type,int>, language} + séparation stricte app-only/wire; résultat {flashcards éphémères, suggestedTags, quota}; variante 2 phases generatePreview→commitPreview(tagIds). Éval IA = {question, userAnswer, cardType, expectedAnswer?, explanation?} → {feedback, suggestedQuality 0..5 ADVISORY, isCorrect? (null si ouverte)}; N'ÉCRIT JAMAIS de SRS (note = voie unique reviewCard); fallback 'évaluer sans IA'; garde dispose anti-course. PDF = PdfExportResult{bytes, suggestedFileName, mimeType}; PAS de printing; partage via seam injectable (share_plus app-side). Suppression carte lex = soft-delete SANS purge du RepetitionInfo (orphelin toléré); cascade seulement au niveau dossier.
- (constraint) FAITS ZCRUD: sélection (ZStudySessionSelector/Config kernel: folderId/tagIds/types/count) déjà séparée des runtimes; moteurs consomment une file résolue; voie SRS unique grade→ZSessionReviewer(=reviewCard)→ZSrsScheduler.apply verrouillée; ZWhiteExamSessionEngine = patron 'noté sans SRS par construction' (setup→running→submitted, ZStudySessionResult pur); ZReviewMode.cramming existe déjà au kernel; ATTENTION: mode ne conditionne pas le reviewer dans ZStudySessionEngine. zcrud_export = headless bytes-only (Syncfusion ^32.1.19, PdfBitmap OK; pas de printing/share; ZFileSaver existe). Mindmap: nodeContentBuilder injectable identique graphe+liste; slot rich opt-in ZMindmapMarkdownContent(extra[slotKey] Delta); cellSize FIXE 180x72; outline editor = TextField bruts label+content stables keyés id (point de greffe éditeur riche). Seams prêts: ZItemActionsMenu(zcrud_study), ZToaster/ZDiscardChangesGuard(ui_kit), ZAdaptiveGrid, ZTagChips/Editor, ZcrudLabels, ZSrsConfig.passThreshold(0..5, seuil 3), échelle qualité zcrud = 0..5.
- (constraint) FAIT zcrud (OA-4): ZStudySessionEngine se construit sur une file DÉJÀ sélectionnée (List<ZSessionItem>) + seam ZSessionReviewer (typedef = signature exacte de reviewCard) — jamais ZSrsScheduler.apply en direct. ZWhiteExamSessionEngine n'a AUCUN paramètre reviewer/scheduler (invariant zéro-SRS garanti PAR LE TYPE, prouvé par z_white_exam_no_srs_test) et produit ZStudySessionResult via seam pur ZExamScoringPort. ZReviewMode {spaced,learn,list,test,whiteExam,cramming} existe; cramming SANS moteur dédié.
- (constraint) FAIT zcrud (OA-6): zcrud_export sort des Uint8List (jamais fichier/type Syncfusion en public); syncfusion_flutter_pdf ^32.1.19 confiné data/; ZPdfCreationService.buildFromImages + PdfBitmap/drawImage EXISTENT (insertion image OK); AUCUNE dep printing/pdf → pas de preview/impression/partage; ZFileSaver écrit/télécharge seulement. Maillon manquant OA-1 = pont LaTeX→image.
- (constraint) FAIT zcrud (OA-2): ZMindmapView a DÉJÀ un slot nodeContentBuilder (ZMindmapNodeContentBuilder) appliqué identiquement graphe+liste, défaut ZMindmapDefaultNodeContent sans dep markdown; ZMindmapMarkdownContent.builder(slotKey:) existe (payload Delta dans node.extra, AD-4). MAIS cellSize FIXE 180x72 (aucune mesure intrinsèque) et compact court-circuite le builder. ZMindmapOutlineEditor n'a AUCUN slot de champ: TextField label+content construits en dur dans _OutlineRow → greffe = nouveau paramètre builder.
- (constraint) FAIT zcrud (OA-1): rendu markdown/LaTeX INTÉGRALEMENT couplé à Quill (ZMarkdownReader readOnly + embeds flutter_math_fork ^0.7.4 NON exportés). Aucun renderer markdown→widget hors Quill. Seams prêts: ZcrudLabels(resolve/maybeResolve via ZcrudScope), ZToaster+ZToasterScope, ZDiscardChangesGuard(isDirty/child), ZAdaptiveGrid+computeCrossAxisCount, ZItemActionsMenu (dans zcrud_study, data-driven), ZTagChips/ZTagEditor, ZSrsConfig.passThreshold=3, ZSrsQualityButtons(ZQualityScale).
- (constraint) FAIT lex (OA-3): l'ordre manuel est une ENTITÉ SÉPARÉE FolderContentsOrder{folderId, Map<sectionKey, List<String> ids>} (PAS un champ position sur l'item), sectionKey = '<kind>' ou '<kind>/<subFolderId>'; applyOrder = mémorisés d'abord puis nouveaux APPENDÉS stable, orphelins ignorés; persistée users/{uid}/study_content_orders/{folderId} (état PERSONNEL, hors sous-arbre partagé); DEUX modalités de réordonnancement (drag + boutons Monter/Descendre a11y) partageant reorderSection(folderId, sectionKey, orderedIds).
- (constraint) FAIT lex (OA-5): génération = requête {source, count 1..50, typesDistribution, language} en WIRE camelCase SANS uid (identité par token); source/targetFolder/noteText app-only; résultat = flashcards ÉPHÉMÈRES (aucun id ni source du backend, matérialisation client) + suggestedTags + quota; flux 2-phases generatePreview→commitPreview(tagIds). Éval réponse = {question,userAnswer,cardType,expectedAnswer?,explanation?} → {feedback, suggestedQuality 0..5 ADVISORY, isCorrect?(null si ouverte), quota} — N'ÉCRIT JAMAIS le SRS (reviewCard reste la voie unique), errorKind typé {offline,quotaExceeded,rateLimited,serviceUnavailable,network,server}, garde _disposed. PDF = {bytes, suggestedFileName, mimeType} + PARTAGE système seul (share_plus via seam PdfShareSink), AUCUN printing/preview.
- (constraint) FAIT lex (dette): la suppression d'UNE flashcard NE PURGE PAS son RepetitionInfo (orphelins); la cascade study_repetitions n'existe qu'à la suppression de DOSSIER. IFFD purge par where flashcardId==id AVANT delete (fire-and-forget, erreurs avalées, PAS de lots de 10 contrairement au rapport).
- (constraint) FAIT iffd (OA-5): éval = {question,userAnswer,correctAnswer,questionType,timeTakenSeconds,previousHints[]} → score BRUT 1-5 en TEXTE (à parser/valider), pas de justification (explication = appel séparé); repli déterministe: QCM/VF 5 si exact sinon 1, ouverte/exercice 3 neutre, 'je ne sais pas' = 1 sans appel IA. Hint = {question,correctAnswer,existingHints[]} → texte; IA appelée SEULEMENT après épuisement de l'indice stocké (flashcard.indice = indice n°0), nouveaux indices NON persistés (in-memory). Génération 3 modes {document+pagesContents, sujets/tags, notes texte} → JSON 'sale' à normaliser; questionsCounts défaut modèle {QCM:5,VF:4,ouverte:3,exercice:3} MAIS écran divergent {ouverte:5,QCM:4,VF:3,exercice:3} et wrapper de sélection ORPHELIN (non câblé) → source unique à décider.
- (constraint) FAIT iffd (streak): sémantique = DATE CALENDAIRE LOCALE (DateTime.now() local, heure ignorée, pas d'UTC); idempotent par jour (lastStudy==today → no-op); +1 si today.difference(lastStudy).inDays==1 sinon reset à 1; persisté sur le user {currentStreak:int, lastStudyDate:DateTime? en ms epoch}; exclu en mode listOnly; échec silencieux. Divergence PRD: PRD dit 'remise à zéro' → en réalité reset à 1 (le jour courant compte). Risque multi-device (fuseau) à trancher.
- (decision) AD(éval): port d'évaluation = contrat lex ENRICHI. Entrée {question, userAnswer, cardType, expectedAnswer?, explanation?, timeTaken?, hintsUsed?}; sortie typée {feedback, suggestedQuality (échelle ZSrsConfig), isCorrect?}. ADVISORY STRICT: pré-sélectionne un bouton SRS, jamais d'écriture SRS (reviewCard voie unique). QCM/VF évalués LOCALEMENT (déterministe/offline), jamais par IA. Prévient: divergence lex(feedback+advisory) vs iffd(score brut auto-appliqué).
- (decision) AD(ordre): ordre manuel = entité séparée GÉNÉRIQUE ZContentOrder{scopeId, Map<sectionKey, List<id>>} (portée du modèle lex FolderContentsOrder, dé-flashcardisée), état PERSONNEL hors entité partagée (comme le SRS, AD-19/AD-9); append stable des nouveaux, orphelins ignorés; drag ET boutons Monter/Descendre partagent la voie d'écriture unique reorderSection(scopeId, sectionKey, orderedIds). Interdit: champ position inline sur l'item.
- (decision) AD(cascade): toute suppression (unitaire ET lot FR-SU19) passe par la cascade déclarative AD-21 existante, AWAITED, avec rapport d'échecs par élément (AD-10) — zcrud corrige la dette lex (orphelins RepetitionInfo) et iffd (fire-and-forget, erreurs avalées). Prévient: deux voies de suppression divergentes entre les stories.
- (decision) AD(streak): reset à 1 (pas 0 — la répétition du jour compte), jour civil LOCAL, idempotent par jour, horloge+fuseau PARAMÉTRÉS (calcul pur testable), exclu en mode consultation. Corrige l'imprécision 'remise à zéro' du PRD (FR-SU11) → amender le PRD en aval.
- (decision) AD(rendu riche): patron du nodeContentBuilder généralisé — TOUT widget affichant du contenu de carte/nœud expose un slot de rendu INJECTABLE, défaut = texte brut thématisé (zéro dep). zcrud_flashcard/zcrud_mindmap ne dépendent JAMAIS de Quill/flutter_math_fork. EN PLUS: zcrud_markdown fournit les ADAPTATEURS PRÊTS à injecter (sur le modèle de ZMindmapMarkdownContent.builder(slotKey:)) pour la carte de révision ET l'éditeur outline — l'app injecte, n'écrit rien. Nouveau: l'outline editor doit gagner un slot de CHAMP d'édition (aujourd'hui TextField en dur dans _OutlineRow).
- (decision) AD(export): zcrud_export reste PUR (bytes in/bytes out, aucune dep plateforme). La preview/impression/partage canonique vit dans un NOUVEAU package satellite optionnel zcrud_export_ui (dep printing) — conséquence zéro pour qui ne l'importe pas (patron AD-1/AD-8: Syncfusion isolé derrière zcrud_list). CONSÉQUENCE: amender la contre-métrique PRD 'pas de nouvelle dep au-delà des 2 décidées' (+printing confiné à zcrud_export_ui) et OA-6.
- (decision) AD(génération IA): requête canonique = union lex+IFFD = {source (registre AD-4 pluggable: document+pages, sujets, texte libre, article, note, conversation), count borné, typesDistribution, language, instructions?, modelId: String? OPAQUE}. zcrud TRANSPORTE modelId sans jamais l'interpréter; catalogue/libellés/cascade de repli = app-side (jamais de nom de modèle dans zcrud). Résultat = cartes ÉPHÉMÈRES (aucun id/source backend) + suggestedTags + quota?, matérialisation client après revue (flux 2-phases preview→commit).
- (decision) AD(défauts génération): source UNIQUE des défauts de typesDistribution côté zcrud (corrige la divergence iffd modèle{QCM:5,VF:4,ouverte:3,exercice:3} vs écran{ouverte:5,QCM:4,VF:3,exercice:3} + wrapper orphelin) — répartition équitable pure façon lex distributeTypes(count, types) comme défaut calculé, surchargeable.
- (decision) AD(cramming, tranche OA-4): cramming = ZStudySessionEngine + ZSessionReviewer NO-OP CANONIQUE fourni par zcrud_session (retourne Right sans persister) → rythme drill carte-par-carte + lapses, zéro écriture SRS. Le no-op est un seam FOURNI (jamais bricolé par l'app) → une story ne peut pas câbler un vrai reviewer par erreur. Invariant à tester: aucun mode non-SRS (list/test/whiteExam/cramming) n'atteint reviewCard.
- (decision) AD(label riche graphe, tranche OA-2): rendu riche BORNÉ à la cellule graphite fixe (défaut 180x72, cellSize configurable par l'app), troncature/clip propre — jamais de mesure intrinsèque ni de re-layout (graphite ne le supporte pas). Mode compact garde le label brut (comportement existant préservé). Rendu riche COMPLET garanti dans l'outline editor + la liste a11y.
- (override) AD-34 RÉVISÉ (proposition user, retenue): cramming = ZCrammingSessionEngine DÉDIÉ (aucun paramètre reviewer/scheduler → zéro-SRS garanti PAR LE TYPE, comme ZWhiteExamSessionEngine, conforme à AD-23 'par construction') et NON plus cycle+reviewer no-op. Rationale du revirement: l'objection 'duplication' tombe — la logique de cycle est déjà un réducteur pur top-level (reduceGrade) + value-object (ZSessionState) que le moteur dédié RÉUTILISE ; à l'inverse le no-op est une VALEUR passable, donc l'invariant n'était garanti que par convention+test, pas par le type. Coût assumé: un type public de plus. Aucun ZSessionReviewer no-op ne sera fourni (évite qu'il serve de porte dérobée).
- (change) Gate rubrique+réconciliation appliqué: (HIGH) AD-34 étendu en TABLE mode→moteur (list+cramming→ZCrammingSessionEngine, test+whiteExam→ZWhiteExam, spaced/learn seuls avec reviewer) — comble le trou list/test sans moteur assigné; (HIGH) E-MULTI-EDIT doté d'invariants: AD-43 frontière brouillon/persistance (réconcilie AD-39 vs cycle brouillon FR-SU20), AD-44 sélection à propriétaire unique + actions déclarées + champ commun dérivé du ZFieldSpec + Déplacer sur champ de rattachement déclaré; (MEDIUM) AD-45 lecture seule/duplication (FR-SU21, éphémère, isReadOnly reset, aucun état personnel copié); (MEDIUM) section 'Écarts assumés vs PRD' (cramming/streak/printing) ajoutée; (MEDIUM) enveloppe de distribution melos+gates pour zcrud_export_ui (NFR-SU10); intention 'généricité au juste besoin' restaurée en convention; échelle de qualité unique + clamp (AD-35); plancher d'indices précisé (AD-36); défauts ZCardAdvanceBehavior par mode; AD-8 ajouté aux binds.
- (override) AD-34 RE-RÉVISÉ après vérif disque (gate reality-check, CONFIRMÉ par moi): ZLinearSessionState (ES-4.3, zcrud_session/domain, exporté, testé z_linear_no_srs_test) EST DÉJÀ le runtime list/cramming — aucun paramètre review/scheduler, assert de mode, réducteurs purs advanceLinear/requeueCramming, zéro-SRS par construction. Donc AUCUN ZCrammingSessionEngine n'est créé (il aurait dupliqué un runtime livré): on RATIFIE les 3 runtimes existants (ZStudySessionEngine spaced/learn, ZLinearSessionState list/cramming, ZWhiteExamSessionEngine test/whiteExam). Le memlog 'cramming sans moteur dédié' était FAUX (absence jamais vérifiée par grep négatif).
- (decision) TROU SYMÉTRIQUE à fermer (F3 vérifié): ZStudySessionEngine accepte n'importe quel ZReviewMode avec un vrai reviewer → (mode:cramming, reviewer:réel) constructible = écriture SRS sur mode non-SRS. Il reçoit la garde symétrique de ZLinearSessionState: n'accepter que spaced/learn.
- (override) AD-40 CORRIGÉ après vérif disque: flash→md et mind→md sont des deps DURES existantes, et ZMindmapMarkdownContent vit DANS zcrud_mindmap. Faire fournir les adaptateurs PAR zcrud_markdown aurait créé un CYCLE (md→mind) violant AD-1. Règle corrigée: l'adaptateur vit chez le CONSOMMATEUR (mindmap et flashcard), au-dessus de leur dep existante; zcrud_markdown ne connaît aucun consommateur.
- (decision) AD-46 (nouveau): échelle de qualité = concept de DOMAINE possédé par ZSrsConfig (bornes aux côtés de passThreshold); ZQualityScale (présentation zcrud_session) en DÉRIVE. Échelle canonique 0..5 (choix user, SM-2 complet, défaut actuel, usage lex) → seau 'mauvais' de FR-SU12 ÉLARGI à q0-2 (comble le trou q=0 hors seau). Clamp défensif. Écart PRD (1-5) assumé, à amender.
- (version) CORRECTIF de fait: syncfusion du repo = ^32.1.19 (pas 34.1.31, dernière publiée) — le memlog 'toutes alignées' ne valait PAS pour syncfusion (2 majeures de retard). Hors périmètre de ces epics (aucun FR n'exige un bump) → consigné en Deferred. printing ^5.15.0 VÉRIFIÉ: accepte des bytes bruts (LayoutCallback/sharePdf), fix CVE-2024-4367, tire pdf ^3.13.0 (confiné au satellite), exige flutter>=3.41/dart>=3.12 — monorepo déjà sur sdk ^3.12.2 / Flutter 3.44.4 → AUCUN bump forcé.
- (change) AD-42 précisé: buildFromImages = UNE IMAGE PAR PAGE; le gabarit FR-SU16 exige EN PLUS une composition inline texte+bitmap dans la page — deux maillons manquants, pas un.
- (change) Findings restants appliqués: AD-36 propriétaire unique de la pénalité d'indices (plafond local s'applique EN DERNIER sur la valeur rendue; hintsUsed informatif au port) — évite double/zéro pénalité; AD-39 granularité de rapport arbitrée à l'ÉLÉMENT RACINE (concilie borne ≤450 et rapport par élément); diagramme mermaid corrigé aux arêtes RÉELLES (md→flash, md→mind, exp→flash); Deferred + retard syncfusion ^32.1.19 vs 34.1.31. Lint: 0 finding. AD réordonnés 33..46 (ids stables, ordre croissant); 14/14 portent Binds/Prevents/Rule.
- (event) spine finalized
