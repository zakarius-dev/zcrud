---
scope: Parité DODLP totale du formulaire — placement des adaptateurs de parité (satellites/bindings), dateRange cœur, fork awesome_select, isolation WYSIWYG, tokens d'aération. FR-1..40 / NFR-1..9.
purpose: build-substrate
altitude: epic
updated: 2026-07-18T05:16
---

- (constraint) Hérité fondateur AD-1..AD-16 (architecture-zcrud-2026-07-09), read-only. Binds ici surtout: AD-1 (acyclique, CORE OUT=0), AD-2/AD-15 (réactivité Flutter-native, aucun état de formulaire global), AD-3/AD-10 (codegen défensif, additif), AD-4 (registres ouverts injectés), AD-6 (bindings), AD-7 (rich-text codec pluggable, controller isolé), AD-8 (dep lourde isolée derrière abstraction cœur), AD-12 (zéro secret), AD-13 (RTL/a11y >=48dp), AD-14 (pureté domaine).
- (constraint) Hérité study-ui AD-33..AD-46 (architecture-zcrud-study-ui-2026-07-16), read-only. Patrons réutilisés: AD-8/AD-42 (satellite optionnel dédié à dep lourde, ex zcrud_export_ui/printing), AD-40 (adaptateur riche chez le consommateur, jamais un cycle), enums>booléens pour les variantes multi-app.
- (constraint) VERROUILLÉ owner: awesome_select adopté via un FORK maintenu par nous (élimine le ref:master flottant de DODLP), enveloppé derrière un ZFieldWidgetBuilder dans un satellite, jamais zcrud_core. Mécanique (fork épinglé vs vendoring) = à trancher ici.
- (constraint) VERROUILLÉ owner: PARITÉ DODLP TOTALE, WYSIWYG HTML inclus (html_editor_enhanced). Livraison phasée (MVP/Média-rich/Finitions) mais parité-cible non négociable.
- (constraint) VERROUILLÉ owner: toute story touchant zcrud_core (dateRange, color multiple, itemsAreTags, tokens d'aération, nouveaux enum) est SÉRIALISÉE — une seule story écrit zcrud_core à la fois.
- (version) awesome_select 6.0.0 sur pub.dev — publié ~3 ans, effectivement non maintenu; DODLP en dépend via fork GitHub ref:master flottant. Licence MIT (à confirmer LICENSE conservé). => argument fort pour vendoring.
- (version) html_editor_enhanced 2.7.1 (~12 mois, WebView InAppWebView + Summernote). Alternative maintenue: html_editor_plus 0.0.7 (WIP, flutter_inappwebview ^6.1.0). flutter_html 3.0.0 (~16 mois, maintenu, rendu lecture).
- (version) Média: image_cropper 12.2.1 (3 mois, BSD-3). pinput 6.0.2 (5 mois) pour PIN. flex_color_picker ^3.7.1 (roue HSV, reste binding). camera/image_picker/video_thumbnail/file_picker/open_file/dotted_border: pins délégués à l'impl du satellite.
- (event) constraints + versions loggés
- (decision) AD-47 dateRange NATIF au cœur. Binds FR-5. Prevents: un satellite superflu pour une capacité sans dep lourde; deux emplacements du type. Rule: EditionFieldType.dateRange (camelCase, près de dateTime/time); valeur ZDateRange{start,end} domaine-pur ISO-8601, invariant end>=start validé, fromJsonSafe->null, null toléré si optionnel (AD-10); widget natif ZDateRangeFieldWidget sous ZFieldListenableBuilder via Material showDateRangePicker (AUCUNE dep table_calendar/date_time_picker); zcrud_generator émet le ZFieldSpec; story cœur sérialisée; test rétro-compat vert. CORE OUT=0 préservé (showDateRangePicker = primitive Material).
- (decision) AD-48 Parité de sélection riche par SEAM PRÉSENTATEUR injecté, jamais le widget registry. Binds FR-6,7,8,9. Prevents: un adaptateur qui tente de servir select/radio/checkbox/relation via ZWidgetRegistry alors que familyOf pré-route ces familles de base vers ZSelectFieldWidget/ZRelationFieldWidget AVANT registryOrFallback (le builder ne serait jamais atteint); deux propriétaires du rendu modal. Rule: zcrud_core déclare une ABSTRACTION Material-free ZSelectPresenter (patron AD-8/ZListRenderer/ZcrudScope.colorPicker) résolue via ZcrudScope; défaut = modal natif zcrud actuel. Les familles natives délèguent au présentateur injecté si présent. zcrud_select fournit le présentateur adossé au fork awesome_select (SmartSelect: modal S2 + recherche + CRUD inline via ZRelationSourceRegistry/ZRelationCrudRegistry). CORE OUT=0 (cœur = abstraction seule).
- (decision) AD-49 awesome_select VENDORISÉ (pas fork git épinglé). Binds FR-6,7,8,9; NFR-2,9. Prevents: une dépendance-de-dépendance git vers un hôte tiers à un ref étranger/flottant chez un consommateur en dép. git; du code hors des gates du monorepo. Rule: le source du fork entre comme MEMBRE DE WORKSPACE melos privé (publish_to:none) sous packages/awesome_select/, résolu offline sous notre tag, versionné avec nos releases, soumis aux gates repo (analyze, CORE OUT=0, secrets, codegen-distribution). Dépendu UNIQUEMENT par zcrud_select. Les types awesome_select ne fuient JAMAIS dans une signature publique. LICENSE MIT conservé (attribution). Rejeté: git: ref figé (partiellement hors gates, host tiers au build consommateur). Motif retenu = conservateur (contrôle, offline, gates).
- (decision) AD-50 WYSIWYG HTML isolé dans satellite zcrud_html; WebView = controller isolé (patron AD-7). Binds FR-22,23; NFR-1,2. Prevents: la 2e voie d'état WebView cassant SM-1/AD-2; une collision de registration avec la voie Delta de zcrud_markdown. Rule: zcrud_html enregistre html/inlineHtml sur ZWidgetRegistry; l'adaptateur est un State possédant le HtmlEditorController/WebView créé UNE FOIS en initState (jamais recréé au rebuild de tranche), key ValueKey('z-html-<field.name>'); lit ctx.value (HTML String) comme contenu initial; pousse ctx.onChanged UNIQUEMENT sur onChange/blur débouncé (jamais synchrone par frappe); re-sync depuis ctx.value seulement hors focus (garde recon §5). Format persisté = HTML String (PAS forcé en Delta — c'est la raison d'être vs la voie markdown); flutter_html en rendu lecture; pertes de round-trip bornées et documentées; AD-10 défensif (HTML corrompu -> éditeur vide). zcrud_html et zcrud_markdown enregistrent html/inlineHtml de façon MUTUELLEMENT EXCLUSIVE (collision=throw): l'app choisit une seule voie au bootstrap.
- (decision) AD-51 Adaptateurs média dans satellite dédié zcrud_media (patron AD-8/zcrud_export_ui), pas dans le binding ni le cœur. Binds FR-15,16,17,18; NFR-2,9. Prevents: des pickers média couplés à GetX (non réutilisables pour Riverpod/lex_douane); une dep de plateforme fuyant dans zcrud_core. Rule: zcrud_media câble le contrat CŒUR EXISTANT ZFilePicker/ZFileSource/ZAppFileField (bottom-sheet multi-sources, caméra, recadrage image_cropper, scan->PDF, vignette video_thumbnail, zone de dépôt dotted_border, open_file); aucun type de plateforme dans son API publique (Uint8List/chemins neutres); dépend seulement de zcrud_core; le binding zcrud_get le COMPOSE (injecte l'implémentation dans le seam). Réutilisable par tout binding.
- (decision) AD-52 Nouvelles formes de valeur cœur, additives, sérialisées, défensives. Binds FR-13,20; NFR-5,6. Prevents: divergence de (dé)sérialisation; deux écritures cœur concurrentes; un parent qui échoue sur une valeur corrompue. Rule: color multiple = ZColorConfig.multiple natif, valeur List<int> ARGB (OQ-2 défaut; PAS de fork color_picker_field), parse défensif de la liste (entrée corrompue ignorée, jamais throw); subItems.itemsAreTags = ZSubListDisplayMode.tags natif, zéro dep. Toute nouvelle valeur d'EditionFieldType est additive (camelCase, @JsonKey unknownEnumValue), le générateur émet le ZFieldSpec, *.g.dart régénérés+commités, test rétro-compat vert; une story cœur à la fois. La roue HSV (flex_color_picker) reste côté binding via ZcrudScope.colorPicker.
- (decision) AD-53 Champs Finitions net-new dans satellite zcrud_field_extras via le registry. Binds FR-34,35,36,37. Prevents: quatre satellites d'un champ chacun (surface NFR-9); des kinds Finitions bricolés en 'custom'. Rule: PIN (pinput), autocomplétion, table éditable (pure-Flutter virtualisée, ListView.builder), icon picker (registre d'icônes) vivent dans UN satellite zcrud_field_extras, servis par ZWidgetRegistry sous leur kind. icon est déjà un EditionFieldType; pin/autocomplete/editableTable ajoutent leur enum value au cœur de façon additive+sérialisée (AD-52) quand planifiés. Valeurs neutres (PIN/autocomplete=String, table=List<Map> défensif). Deps légères confinées au satellite (CORE OUT=0). Phase Finitions; FR-13/FR-37 sans call-site actif -> peuvent rester en repli ZUnsupportedFieldWidget étiqueté ABSENT tant que non planifiés (OQ-6).
- (decision) AD-54 Tokens d'aération au cœur (ZcrudTheme ThemeExtension) + 3 écarts tranchés. Binds FR-38; NFR-3,4. Prevents: des constantes d'aération éparpillées recopiées par chaque binding; une couleur codée en dur. Rule: mesures d'aération = tokens dp directionnels dans zcrud_core (ZcrudTheme.formPadding défaut EdgeInsetsDirectional.all(12) consommé par DynamicEdition quand padding==null; ZResponsiveGrid.runGutter distinct de gutter, replié sur gutter si null, parité 16H/8V), couleurs TOUJOURS dérivées du ColorScheme. 3 écarts tranchés explicitement: (1) gouttière asymétrique -> runGutter additif; (2) spacer inter-champ -> garder la projection zcrud (zFieldGapAfter espace les blocs; interFieldGap=12 posé par binding), divergence de séquence mixte assumée (parité fonctionnelle, pas pixel); (3) padding d'écran -> token formPadding. En-tête 'boîte grise' DODLP NON reproduit (header sobre thémé, FR-26) — parité fonctionnelle; parité visuelle stricte = OQ-1 owner. Binding pose les valeurs DODLP.
- (decision) AD-55 Binding zcrud_get = point de COMPOSITION unique de l'enregistrement. Binds FR-24,25,26,27 (câblage intl/geo), FR-6..23 (composition). Prevents: deux registrations du même kind (ZWidgetRegistry.register throw), deux propriétaires de la composition. Rule: l'app hôte (via binding zcrud_get) construit ET détient LE ZWidgetRegistry, appelle une seule fois chaque registerZ*Fields (intl phone/country/address, geo location/geoArea, markdown, html OU markdown-html exclusif, select présentateur, field_extras), injecte le ZFilePicker média, le seam colorPicker (flex_color_picker), le présentateur select, et les valeurs de tokens d'aération. Jamais d'auto-enregistrement par side-effect d'import (AD-4). Registre injecté via ZcrudScope, jamais un singleton statique.
- (decision) AD-56 Harnais de parité & showcase dans example/ (jamais un package). Binds FR-39,40; NFR-7; SM-1,SM-2,SM-3,SM-4. Prevents: du code de démonstration/fixtures polluant un package distribué; un secret DODLP embarqué. Rule: >=6 formulaires DODLP répliqués + page showcase exhaustive (40 types x variantes x états read-only/désactivé/erreur/RTL/thème) vivent dans l'app example/; données FICTIVES, zéro secret, aucune dépendance backend DODLP; un formulaire de frappe intensive sert de banc SM-1 (100 car -> seul le champ courant reconstruit, zéro perte de focus, incl. le champ WYSIWYG isolé); gaps résiduels étiquetés ABSENT/à combler, jamais masqués.
- (event) AD-47..AD-56 loggés (10 décisions)
- (assumption) OQ-2 color multiple -> variante native ZColorConfig.multiple (AD-52), défaut PRD retenu. Alternative satellite kind:colorMulti laissée ouverte au owner.
- (assumption) OQ-5 WYSIWYG HTML retenu (AD-50); concret html_editor_enhanced ^2.7.1 par défaut, html_editor_plus (fork maintenu) comme voie de remplacement — le satellite abstrait le paquet concret pour qu'il soit substituable.
- (assumption) AD-53 zcrud_field_extras: regroupement conservateur des 4 champs Finitions en un satellite (vs 1 satellite/champ) pour borner la surface NFR-9. icon/PIN/autocomplete/table sans besoin produit fort -> restent en repli ABSENT tant que non planifiés (OQ-6).
- (question) OQ-1 owner: parité visuelle STRICTE des sections (boîte grise MyStickyHeader) vs header sobre thémé (défaut AD-54). Déclenche d'éventuels tokens de section cœur.
- (question) OQ-3/OQ-4 owner: sélecteur pays téléphone inline (défaut) vs dialog; validateur Togo length:8 (défaut) vs 11 — arbitrages de câblage zcrud_intl, sans impact structurel sur le spine.
- (question) OQ-6 owner: combler FR-13 (subItems.itemsAreTags) et FR-37 (icon) sans call-site actif, ou reporter jusqu'à motivation IFFD/DLCFTI.
- (question) OQ-7 PM: figer la shortlist des 6 formulaires du harnais sur couverture max types x axes de risque (à l'ouverture de l'epic harnais).
- (direction) NFR-8 migrations ETL app-side (signature PNG->strokes non réversible; phoneNumber String->E.164) sont HORS packages zcrud — à planifier côté app DODLP, jamais dans les epics zcrud. Non couvert par le spine (nommé comme risque/dépendance).
- (direction) Non-goal assumé: LaTeX fallback SVG flutter_tex (matrice #33) banni par le test d'isolation -> placeholder thémé, jamais un crash (cohérent AD-42 study-ui).
- (event) assumptions/questions/directions loggés
- (version) flex_color_picker 3.8.0 (7 mois, BSD-3, maintenu) — pin ^3.7.1 du spine résout vers 3.8.0, à jour.
- (event) Reviewer Gate: lint_spine OK (0 findings); lentille web-verification OK (awesome_select/html_editor_enhanced/flutter_html/image_cropper/pinput/flex_color_picker/showDateRangePicker tous vérifiés) ; lentille adversariale two-units OK (collision html fermée par AD-50+AD-55 ; registry-vs-présentateur fermée par AD-48 ; propriété des valeurs par AD-47/52). Aucun trou nouveau.
- (event) spine finalized
