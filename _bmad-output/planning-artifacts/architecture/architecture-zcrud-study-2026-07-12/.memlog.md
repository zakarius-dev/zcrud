---
scope: Extension éducative zcrud_study : famille de packages (kernel/note/document/session/exam/study), refactor flashcard/mindmap, adapters firestore, bindings. Étend les 16 AD produit, ne les réécrit pas.
purpose: build-substrate
altitude: epic
updated: 2026-07-12T10:56
---

- (constraint) Hérite AD-1..AD-16 (architecture-zcrud-2026-07-09) comme contraintes read-only NON-NÉGOCIABLES : acyclicité, réactivité Flutter-native, codegen, ZExtension, backend-agnostique, offline-first LWW hors-entité, Either/Stream nu, RTL/a11y, bindings multi-manager. Aucun nouveau AD ne peut les contredire.
- (decision) AD-17 Décomposition fine multi-packages study. Binds FR-S1,NFR-S2,NFR-S10. Prevents duplication historique + import forcé de features non voulues. Rule: nouveaux packages zcrud_study_kernel(bas) < {zcrud_note,zcrud_document,zcrud_session,zcrud_exam} < zcrud_study(orchestration) < bindings ; kernel dépend de zcrud_core seul ; flashcard/mindmap refactorés pour dépendre du kernel ; adapters dans zcrud_firestore ; graphe acyclique prouvé repo-wide (melos analyze+verify).
- (decision) AD-18 Remontée ZStudyFolder option A vers zcrud_study_kernel + refactor non-régressif E9. Binds FR-S1,SM-S2. Prevents cycle (dossier accessible sans tirer flashcard) + régression E9. Rule: ZStudyFolder+validatePlacement+ZFolderContentsOrder+ZStudySessionConfig migrent flashcard→kernel ; flashcard re-dépend du kernel et ne définit plus ZStudyFolder (réexport transitoire toléré) ; mindmap référence par folderId (clé neutre, pas de dep sur l'entité) ; suite E9 verte sans régression (tests>=avant).
- (decision) AD-19 ZSyncMeta hors-entité universel pour TOUTES les entités study (tranche OQ #3 / OQ-S2). Binds FR-S3,FR-S8,NFR-S9. Prevents divergence in-entity vs hors-entity figée dans le canonique. Rule: updated_at+is_deleted hors-entité (ZSyncMeta, AD-9/AD-16) pour toute nouvelle entité study ; merge LWW toujours sur ZSyncMeta.updated_at (jamais T.updatedAt) ; ZStudyFolder.updatedAt in-entity devient miroir de compat déprécié maintenu par l'adapter (non autorité de merge), aligné et documenté, jamais laissé implicite ; ZDocumentAnnotation.isDeleted inline extrait hors-entité.
- (decision) AD-20 ZStudyRepository<T> générique + ZOfflineFirstBoxRepository + ZFirestorePathResolver bi-topologie. Binds FR-S12,FR-S13,FR-S15,SM-S5. Prevents CRUD offline-first dupliqué ~15x + fuite de chemins/Timestamp/Box dans le domaine. Rule: contrat ZStudyRepository<T> (Stream<List<T>> nu, Either<ZFailure,_>, hook validation par override) dans kernel ; helper offline-first + résolveur flat(IFFD)/nested(lex) + collections globales dans zcrud_firestore ; résolution de collection statique explicite (CRUD réflexif banni AD-3) ; merge paramétré par merge-key hors-entité ; ZSyncOrchestrator paramétré par liste injectée.
- (decision) AD-21 Cascade de suppression déclarative bornée (tranche OQ-S6). Binds FR-S14,NFR-S9. Prevents cascade codée en dur non portable entre topologies. Rule: le REGISTRE déclaratif des relations parent/enfant (kind→enfants) vit dans zcrud_study_kernel (neutre, partagé) ; la RÉSOLUTION concrète des chemins/collections de chaque relation vit dans l'adapter zcrud_firestore (via ZFirestorePathResolver) ; batcher borne à <=450 écritures/lot avec flush auto (AD-9).
- (decision) AD-22 Convergence SM-2 : ZSm2Scheduler (E9) source unique (tranche OQ-S3). Binds FR-S17,SM-S1. Prevents 3 SM-2 divergents cassant la compat de planification. Rule: ZSm2Scheduler existant est canonique — il unifie déjà lex Sm2 (plafond EF 2.5) + variante IFFD (clamp des deux bornes), constantes via ZSrsConfig injecté, horloge injectée, échelle qualité clamp 0..5 (absorbe IFFD 1-5) ; reste derrière le port ZSrsScheduler pluggable (jamais sealed), voie d'écriture unique reviewCard()->apply ; le bonus-overdue lex n'est PAS porté par défaut (SM-2 pur) — un app qui le veut fournit une autre impl ZSrsScheduler ; ES-4 fige des tests de contrat (mêmes entrées->mêmes intervalles) et documente la divergence overdue.
- (decision) AD-23 Runtimes de session purs + zéro-écriture-SM2 par construction. Binds FR-S18,FR-S19,FR-S20,NFR-S5. Prevents couplage manager + altération SRS accidentelle en cramming/examen. Rule: ZStudySessionEngine/ZLinearSessionState/ZWhiteExamSessionEngine = classes pures ChangeNotifier/reducer dans zcrud_session, aucun import Riverpod/GetX ; ZLinearSessionState/examen NE référencent PAS le ZRepetitionStore (ports séparés) => zéro apply() garanti par construction et testé ; seule écriture SRS = reviewCard()->ZSrsScheduler.apply.
- (decision) AD-24 ZStudySessionConfig forme unique domaine-pur, égalité profonde au binding (tranche OQ-S4). Binds FR-S33,OQ-S4. Prevents deux formes concurrentes (persistée vs value-object Riverpod). Rule: une seule forme ZStudySessionConfig @ZcrudModel dans kernel (persistable, round-trip) ; l'égalité profonde requise par une family Riverpod vit dans zcrud_riverpod (binding), jamais dans le cœur/kernel.
- (decision) AD-25 Apparence IFFD par sections à scoping ValueListenable isolé + ZFeatureAvailability injectable. Binds FR-S22,FR-S24,SM-S1,SM-S3,NFR-S1. Prevents régression du bug de rebuild global (setState x18) + roadmap d'éditeurs codée en dur. Rule: ZStudyToolsPage = liste de sections paramétriques, CHAQUE section = un scoping ValueListenable/ListenableBuilder isolé (une frappe ne reconstruit que sa section) ; ZFeatureAvailability = interface INJECTABLE (jamais classe const) ; couleurs/labels/l10n injectés (ZcrudScope/ThemeExtension), directionnel/48dp/Semantics/ListView.builder (AD-13).
- (decision) AD-26 Communauté/partage = extension optionnelle activable ; état personnel jamais partagé. Binds FR-S32,NFR-S11. Prevents partage devenu invariant du domaine + fuite d'état personnel + héritage silencieux de la dette de sécurité lex. Rule: partage = ZExtension? sur ZStudyFolder + entités ZStudyMembership/ZShareLink/ZPublicStudyFolder/ZStudyFolderReport + ports ZStudySharingPort/ZStudyModerationPort ; une app qui n'active pas => ni entités ni backend ; état personnel (ZRepetitionInfo/ZFolderContentsOrder/ZDocumentReadingState) séparé du sous-arbre partageable, jamais emporté ; dette sécurité lex (contributeur modifie champs de contrôle ; limite LWW/révocation prochaine sync) corrigée OU documentée explicitement, jamais héritée.
- (decision) AD-27 Migration IFFD flat->canonique + mapping de casse côté adapter + ZSyncMeta additif rétro-compatible. Binds FR-S16,FR-S34,SM-S6,NFR-S4. Prevents perte de données + fuite du mapping de casse dans le domaine + migration cassante. Rule: mapping bidirectionnel snake_case(canonique)<->camelCase(IFFD legacy) UNIQUEMENT dans le codec zcrud_firestore, jamais dans le domaine ; ajout de ZSyncMeta = additif rétro-compatible (doc legacy sans meta -> défauts sûrs) ; asymétrie clock (now local vs serverTimestamp) normalisée dans l'adapter ; gate CI de désérialisation défensive sur corpus IFFD legacy ; FlashcardSource.fromJson diverge de lex (FormatException) vers variant unknown sûr (AD-10).
- (decision) AD-28 Contenus rich-text typés (tranche OQ-S5). Binds FR-S5,FR-S25,FR-S26. Prevents ambiguïté markdown/Delta résolue par heuristique regex + divergence produit du content mindmap. Rule: ZSmartNote.content typé via ZCodec (Delta JSON), jamais String? ambiguë ; le content d'un noeud mindmap reste texte brut dans zcrud_mindmap MAIS le rich-text est un slot ZExtension/ZCodec câblé CÔTÉ APP (opt-in), pas un champ du modèle noeud => IFFD peut migrer avec rich-text sans forcer les autres apps ni modifier zcrud_mindmap ; réutilisation zcrud_markdown tel quel (aucun nouveau codec).
- (event) OQ-S3 tranché: source SM-2 unique = ZSm2Scheduler existant (E9) — vérifié sur code (unifie lex Sm2 EF2.5 + IFFD double-clamp, ZSrsConfig injecté). Reste head-of-epic ES-4: figer tests de contrat de planification + documenter divergence overdue-bonus lex non portée.
- (event) OQ-S4 tranché: forme unique ZStudySessionConfig domaine-pur (AD-24), égalité profonde déléguée au binding zcrud_riverpod.
- (event) OQ-S5 tranché: rich-text content mindmap = slot ZExtension/ZCodec côté app opt-in (AD-28), pas un champ du noeud ; note content typé ZCodec.
- (event) OQ-S6 tranché: registre de cascade déclaratif dans zcrud_study_kernel (relations neutres) + résolution de chemins dans l'adapter zcrud_firestore (AD-21).
- (assumption) Besoin IFFD des examens confirmé par 'v1=TOUT' ; si absent, zcrud_exam reste importable indépendamment (NFR-S10).
- (assumption) Comparaison numérique exacte lex Sm2 vs ZSm2Scheduler (overdue-bonus, arrondis) non rejouable sans le code lex ici — déférée en tête d'ES-4 avec critère: tests de planification identiques entrées->intervalles avant merge.
- (assumption) Décomposabilité de folder_study_tools_page (~1750 l.) en sections paramétriques sans perte d'apparence — à valider par golden/design-review en ES-5.
- (event) spine distilled start
- (direction) check count
- (decision) AD-21 renforcé (reviewer lens 2 adversarial): ownership des arêtes de cascade — chaque arête entrante déclarée par le package enfant qui la porte, composition unique par l'orchestrateur, jamais deux satellites propriétaires d'une même arête.
- (event) Reviewer Gate: lint_spine OK (1 low = faux positif {sourceId}_{mode}, contenu réel) ; lens1 reality-check OK (aucune nouvelle dep, SM-2 vérifié sur code) ; lens2 adversarial → 1 trou fermé (AD-21). spine finalized.
- (event) [2026-07-13 · ES-1.3] OQ #3 / OQ-S2 **TRANCHÉE et MATÉRIALISÉE** (AD-19.1/AD-19.2). Règle normative : « toute NOUVELLE entité study porte updated_at + is_deleted HORS-ENTITÉ via ZSyncMeta ; le merge LWW se fait TOUJOURS sur ZSyncMeta.updated_at, JAMAIS sur un T.updatedAt interne ». Définition MACHINE unique = membres statiques de ZSyncMeta (zcrud_core) : kUpdatedAt/kIsDeleted/reservedKeys/stripReserved — zéro nouveau symbole public top-level, surface du barrel zcrud_study_kernel INCHANGÉE (garde z_kernel_surface_guard_test resté vert sans dérogation ni édition du `hide` de zcrud_flashcard).
- (decision) [ES-1.3] ZSyncMeta est RÉUTILISÉ depuis zcrud_core : AUCUN doublon, AUCUN réexport dans le kernel (un réexport fuiterait dans zcrud_flashcard via le réexport en bloc du barrel kernel). Accès unique : `package:zcrud_core/domain.dart`.
- (event) [ES-1.3] DÉFAUT LATENT CORRIGÉ : `_reservedKeys` de ZStudyFolder ET de ZFlashcard omettaient `is_deleted`. Or les stores écrivent is_deleted DANS le corps puis passent la map COMPLÈTE à fromMap => is_deleted atterrissait dans `extra` et était RÉÉMIS par toMap() (fuite d'une préoccupation de store dans le domaine, AD-16), cassant `==` entre une entité en mémoire et la même relue. Corrigé par `...ZSyncMeta.reservedKeys` dans les deux entités. Non-perte de données : is_deleted appartient au store, réécrit à chaque put et préservé verbatim sur la voie de sync (ZSyncEntry).
- (decision) [ES-1.3] ZStudyFolder.updatedAt = MIROIR DE COMPAT @Deprecated (jamais l'autorité de merge). @Deprecated + @ZcrudField COEXISTENT : vérifié sur disque après `melos run generate` — z_study_folder.g.dart émet TOUJOURS `ZFieldSpec(name: 'updated_at', ...)` (13 specs), donc _reservedKeys reste complet (pas de régression silencieuse). `deprecated_member_use` est de sévérité `info` en cross-package et `melos run analyze` ne passe PAS --fatal-infos => RC=0 préservé (analyze repo-wide : « No issues found » sur les 15 packages).
- (event) [ES-1.3] Divergence résiduelle DOCUMENTÉE (AD-19.2) : le miroir n'est pas un champ distinct mais la MÊME clé persistée `updated_at`, maintenue par collision de clé et ÉCRASÉE par l'adapter à chaque put (hive_z_local_store `_encode` écrit la méta APRÈS le corps) => le miroir n'a aucun pouvoir d'écriture (prouvé, AC5-bis). ZFlashcard.updatedAt reste un miroir NON déprécié (surface E9 consommée par DODLP).
- (event) [ES-1.3] DETTES CONSIGNÉES : DW-ES13-1 (LOW) — 4 sites redéclarent les clés en dur (zcrud_firestore : hive_z_local_store, firebase_z_repository_impl ; zcrud_mindmap : z_mindmap, z_mindmap_node) et devront adopter ZSyncMeta.reservedKeys (ES-1.4 ou nettoyage ES-2). DW-ES13-2 (LOW) — dépréciation formelle de ZFlashcard.updatedAt reportée (à re-statuer en ES-2/ES-11).
- (event) [2026-07-13 · ES-1.3 · REMÉDIATION code-review] H1/H2 CORRIGÉS — la règle AD-19.1 était VIOLÉE par 2 entités le jour même de son écriture : ZRepetitionInfo (zcrud_flashcard, l'« exemplaire de référence » désigné par AD-19.1 lui-même — document persisté top-level study_repetitions/{cardId}, aucun champ updatedAt/isDeleted déclaré donc capturant les DEUX clés réservées dans `extra` et les réémettant par toMap()) et ZStudySessionConfig (zcrud_study_kernel — entité du NOYAU, patron naturel qu'ES-2 aurait copié). Les deux `_reservedKeys` consomment désormais `...ZSyncMeta.reservedKeys`. Tests de non-régression : groupes « AD-19 — clés de sync hors-entité » dans z_repetition_info_test.dart, z_study_session_config_test.dart (kernel + miroir flashcard).
- (event) [ES-1.3 · REMÉDIATION] M1 CORRIGÉ — le contrat PUBLIC du cœur `ZSyncable` (zcrud_core) prescrivait l'INVERSE d'AD-19 (« la valeur peut vivre DANS l'entité, comme StudyFolder.updatedAt »). Dartdoc réécrit : la clé LWW vit TOUJOURS hors-entité (ZSyncMeta) ; un T.updatedAt n'est qu'un miroir de compat sans autorité ; renvoi explicite à AD-19/AD-19.1. Grep repo-wide : aucun autre dartdoc/commentaire ne prescrit encore l'in-entité.
- (decision) [ES-1.3 · REMÉDIATION] M2+M3 CORRIGÉS dans l'ADAPTER (jamais dans le cœur — AD-5 : zcrud_core ne connaît pas Timestamp). FAILLE : la méta pouvait être NEUTRALISÉE à null au décodage (updated_at en Timestamp Firestore natif — format legacy DODLP — ou hinté persistAs:timestamp) => ZSyncMeta._parseIso (String-only) renvoie null => TOUTES les métas null => ZLwwResolver dégénère en « le local gagne toujours » => écritures distantes ÉCRASÉES, silencieusement, sans test rouge. Le test STAR prouvait que la méta PRIME, pas qu'elle SURVIT au décodage. CORRECTIF : (a) FirebaseZRepositoryImpl._inject normalise INCONDITIONNELLEMENT les ZSyncMeta.reservedKeys (Timestamp | DateTime | {_seconds,_nanoseconds}) en String ISO-8601 AVANT tout fromMap/ZSyncMeta.fromJson ; (b) garde MACHINE au constructeur (assert + difference) interdisant qu'une clé réservée entre dans timestampFields. HiveZLocalStore est structurellement immun (persistance JSON : un Timestamp n'y est pas représentable). Preuve : groupe « AD-19 (M3) — la méta SURVIT au décodage d'un document LEGACY » (zcrud_firestore/test/timestamp_hint_test.dart), dont un test rejoue le merge COMPLET (distant legacy Timestamp 2026 vs local 2020 => adoptRemoteIntoLocal ; avant correctif le local gagnait). Documenté en AD-19.2 pts 6-7.
- (decision) [ES-1.3 · REMÉDIATION] M4 CORRIGÉ — AD-19.1.a ajoutée : les clés persistées `updated_at`/`is_deleted` sont RÉSERVÉES AU STORE ; aucun champ métier ne peut les porter (le store les écrase à chaque put => perte de donnée silencieuse). Un horodatage métier légitime utilise une clé DISTINCTE (edited_at, published_at, reviewed_at, content_updated_at) ; un drapeau métier utilise archived_at/is_archived. Table de décision fournie pour ES-2 (ZStudyDocument/ZSmartNote/ZExam).
- (event) [ES-1.3 · REMÉDIATION] L1 CORRIGÉ (chemin de ZRepetitionInfo : .../lib/src/domain/z_repetition_info.dart, pas .../lib/src/data/z_repetition_store.dart) + reformulation : l'exemplaire n'était PAS conforme avant H1. L5 CORRIGÉ (test STAR : 2 cas d'asymétrie de null — méta locale null / distante datée et l'inverse — le miroir ne départage RIEN).
- (event) [ES-1.3 · REMÉDIATION] DW-ES13-1 SOLDÉE (au-delà du périmètre initial, coût nul) : les 4 sites à littéraux durs (zcrud_firestore ×2, zcrud_mindmap ×2) consomment désormais ZSyncMeta.kUpdatedAt/kIsDeleted/reservedKeys. Plus AUCUN littéral de clé de sync dans le repo hors z_sync_meta.dart => plus de dérive possible si la méta gagne une clé.
- (event) [ES-1.3 · REMÉDIATION] M5 REPORTÉ à ES-1.4 (justifié) : aucune application MACHINE d'AD-19.1 — rien ne casse si une entité oublie `...ZSyncMeta.reservedKeys` (c'est ce qui a laissé passer H1/H2 sous 1193 tests verts). Le gate repo-wide est un livrable de la story des gates CI (ES-1.4) ; l'implémenter en ES-1.3 dupliquerait l'infrastructure de gates qu'ES-1.4 doit poser. Spécification FIGÉE en AD-19.1.c : pour chaque `kind` du ZcrudRegistry, décoder une map portant les clés réservées et vérifier que `extra` n'en contient aucune ET que `encode()` n'en réémet aucune (allowlist legacy : ZStudyFolder/ZFlashcard pour `updated_at` uniquement). LOW non traités et consignés : L2 (asymétrie @Deprecated ZStudyFolder/ZFlashcard — DW-ES13-2), L3 (les *.g.dart lisent le membre déprécié — piste : ignore_for_file émis par zcrud_generator), L4 (ZSyncMeta.stripReserved sans appelant de production — trouvera son usage au gate ES-1.4).
