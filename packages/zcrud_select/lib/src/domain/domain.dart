// Couche DOMAINE (pure) du satellite `zcrud_select` (fp-1-2, AD-48).
//
// 🔴 Substrat : matérialise la couche `domain` de l'hexagone (contrats/VO purs
// du présentateur de sélection). Vide au stade squelette — sera peuplée en
// fp-4-1 (ex. `ZSelectMode`, spec de choix). Aucune dépendance hors
// {flutter, zcrud_core}. Fichier documenté sans symbole (placeholder).
