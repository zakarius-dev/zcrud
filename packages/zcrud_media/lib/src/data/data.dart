// Couche DATA (adaptateurs) du satellite `zcrud_media`.
//
// Matérialise la couche `data` de l'hexagone. L'adaptateur média (impl de
// `ZFilePicker`) et les seams par défaut adossés aux plugins tiers
// (image_picker/file_picker/image_cropper/…) vivent dans
// `z_media_file_picker.dart` et `z_media_plugin_seams.dart`, à côté de ce
// fichier — leurs dépendances y restent confinées. Fichier documenté sans
// symbole.
