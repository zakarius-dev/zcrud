# ─────────────────────────────────────────────────────────────────────────────
# FORK VENDORISÉ — `awesome_select` (fp-1-2, AD-49).
#
# Source amont : pub.dev `awesome_select` 6.0.0 (MIT), par Akbar Pulatov
#   https://github.com/akbarpulatov/flutter_smart_select
# Copyright (c) 2021 Akbar Pulatov — voir `LICENSE` (conservée à l'identique).
#
# POURQUOI VENDORISÉ : membre de workspace zcrud PRIVÉ (`publish_to: none`),
# résolu OFFLINE sous notre tag, feuille dépendue du SEUL `zcrud_select` (AD-49).
# ⚠️ CORRECTION 2026-07-20 — l'affirmation « la logique amont n'est pas éditée /
# fork substituable » qui figurait ici était FAUSSE et a été retirée. Vérifié par
# diff contre `github.com/akbarpulatov/flutter_awesome_select@master`
# (088e8b7) : 10 fichiers de `lib/` divergent. Le fork PORTE DES CORRECTIFS :
#   • `S2ChosenData<T>` : `abstract class` → `abstract mixin class` (Dart 3 exige
#     un mixin pour une classe utilisée en `with`) ;
#   • `ThemeData.errorColor` → `ColorScheme.error` (retiré de Flutter M3) ;
#   • généricité/nullabilité resserrées (`S2SingleSelection<T?>` → `<T>`,
#     `S2OptionProp<E, String?>` → `String`).
# 🔴 NE PAS remplacer ce vendor par une dépendance git sur l'amont : `master`
# porte une RÉGRESSION VIVANTE — `load(S2ChoicesTask _task)` y fait `task = task;`
# (auto-affectation du getter ⇒ no-op, l'état de tâche n'est jamais mis à jour),
# là où ce fork fait `task = _task;`. La suite de `zcrud_select` reste VERTE avec
# l'amont (elle n'exerce pas le chargement asynchrone de choix) : le vert ne
# protège PAS de cette bascule. Tentative faite et annulée le 2026-07-20.
# Seul ce `pubspec.yaml` est par ailleurs réécrit pour l'alignement workspace :
#   • `publish_to: none` + `resolution: workspace` ;
#   • `environment.sdk` aligné `^3.12.2` (l'amont ciblait `>=2.17.0 <3.0.0`) ;
#   • `flutter_lints` remonté `^5.0.0` (l'amont `^2.0.1` dépend de `lints ^2.0.0`,
#     en conflit avec le `lints ^5.0.0` partagé du workspace) — l'`analysis_options.yaml`
#     amont (`include: package:flutter_lints/flutter.yaml`) est CONSERVÉ.
# Les deps runtime amont sont conservées : `flutter (sdk)` + `collection ^1.16.0`
# (`collection` est un paquet standard Dart, non-`zcrud_*` → invisible graph_proof).
# AD-12 : ZÉRO secret. Aucun `git:`/`hosted:` étranger (résolution offline pure).
# ─────────────────────────────────────────────────────────────────────────────
name: awesome_select
description: Easily convert your usual single/multiple select or dropdown into page, dialog, or bottom sheet with various choice input such as radio, checkbox, switch, chips, or custom widget.
homepage: https://github.com/akbarpulatov/flutter_smart_select
version: 6.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
