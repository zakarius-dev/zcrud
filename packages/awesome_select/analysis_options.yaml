# analysis_options.yaml du FORK VENDORISÉ `awesome_select` (fp-1-2/fp-4-1, AD-49).
#
# 🔴 CODE TIERS — fork MAINTENU par nous (AD-49). L'`include` amont
# (`package:flutter_lints/flutter.yaml`) est CONSERVÉ à l'identique.
#
# fp-4-1 : la compat Flutter/Dart courante a été RÉTABLIE EN SOURCE (mapping M3 :
# `ThemeData.errorColor` → `ColorScheme.error` ; `TextTheme.headline6` →
# `titleLarge` ×2 ; `abstract class S2ChosenData` → `abstract mixin class` pour
# Dart 3). Le BLANKET par catégorie précédent (`undefined_getter: ignore` +
# `class_used_as_mixin: ignore`) est donc RETIRÉ : `dart analyze` doit repasser
# RC=0 sans rabaissement global — `zcrud_select` importe désormais ce paquet et
# les vrais diagnostics DOIVENT redevenir visibles. Tout diagnostic résiduel
# inévitable serait traité par un `// ignore: <règle_précise>` ciblé ligne-à-ligne
# (jamais par catégorie/fichier) — il n'en subsiste aucun.
include: package:flutter_lints/flutter.yaml
