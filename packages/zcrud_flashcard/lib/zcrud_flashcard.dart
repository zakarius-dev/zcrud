/// Barrel d'API publique de `zcrud_flashcard`.
///
/// `ZFlashcard` + SRS (`ZSrsScheduler`).
///
/// API publique = ce barrel ; implémentation sous `lib/src/`.
library;

export 'src/domain/z_flashcard_api.dart';
